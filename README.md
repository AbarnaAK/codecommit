* Create repo for code commit
* Upload files into repo
* Code build
* Deploy the code
* Create IAM user
* Artifacts
* Install code
* Create Pipeline
* If any changes in the file, pipeline will automatically detected and make changes.
